Run via Bash: `python3 scripts/skill_router skills/design/brief/SKILL.md --args '$ARGUMENTS'`
If the script exits non-zero (LM Studio unavailable or model not loaded), fall back to reading `skills/design/brief/SKILL.md` and executing it yourself.

$ARGUMENTS
