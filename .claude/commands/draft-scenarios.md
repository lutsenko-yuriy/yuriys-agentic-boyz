Run via Bash: `python3 scripts/skill_router skills/verify/draft-scenarios/SKILL.md --args '$ARGUMENTS'`
If the script exits non-zero (LM Studio unavailable or model not loaded), fall back to reading `skills/verify/draft-scenarios/SKILL.md` and executing it yourself.

$ARGUMENTS
